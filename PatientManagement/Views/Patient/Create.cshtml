@model PatientManagement.Models.Patient

@{
    ViewData["Title"] = "Create";
}

<div class="max-w-md mx-auto mt-12 mb-12 p-6 bg-white rounded shadow">
    <h2 class="text-2xl font-bold mb-6 text-center">Add New Patient</h2>

    <form asp-action="Create" method="post" class="space-y-4">
        <div>
            <label class="block mb-1 font-semibold">Name</label>
            <input asp-for="Name" placeholder="Enter Patient Name" class="w-full border px-3 py-2 rounded focus:outline-none focus:ring-2 focus:ring-blue-500" />
            <span asp-validation-for="Name" class="text-red-600 text-sm"></span>
        </div>

        <div>
            <label class="block mb-1 font-semibold">Mobile</label>
            <input asp-for="Mobile" placeholder="Enter Mobile Number" class="w-full border px-3 py-2 rounded focus:outline-none focus:ring-2 focus:ring-blue-500" />
            <span asp-validation-for="Mobile" class="text-red-600 text-sm"></span>
        </div>

        <div>
            <label class="block mb-1 font-semibold">Address</label>
            <input asp-for="Address" placeholder="Enter Address" class="w-full border px-3 py-2 rounded focus:outline-none focus:ring-2 focus:ring-blue-500" />
        </div>

        <div>
            <label class="block mb-1 font-semibold">Symptoms</label>
            <textarea asp-for="Symptoms" placeholder="Enter Patient's Case History & Symptoms" rows="3" class="w-full border px-3 py-2 rounded focus:outline-none focus:ring-2 focus:ring-blue-500"></textarea>
            <span asp-validation-for="Symptoms" class="text-red-600 text-sm"></span>
        </div>

        <div class="flex gap-2 justify-between pt-4">
            <a asp-action="Index" class="flex items-center gap-2 bg-gray-600 hover:bg-gray-700 text-white px-4 py-2 rounded shadow transition">
                <i data-lucide="circle-arrow-left" class="w-5 h-5"></i>
                Back to List
            </a>

            <button type="submit" class="flex items-center gap-2 bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded shadow transition">
                <i data-lucide="circle-plus" class="w-5 h-5"></i>
                Create
            </button>            
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
